# md → tex mirror pass 2 (AIW-211 verify, AIW-161 mirror, §4.2.2 clause mirror)

Source of truth: `paper/full/four-model-theory-full.md`
Build source edited: `paper/full/latex/paper.tex`, `paper/full/latex/references.bib`
Canonical `paper/full/latex/paper.pdf` was **not** touched. All builds went to `tmp/build-full/`.

**Verdict: all three changes are mirrored, and the built PDF proves it. One pre-existing
defect found in the paragraph I edited — reported, not fixed.**

---

## Baseline (measured before any edit)

Ran `scripts/build_full_pdf.py` on untouched sources first, so any regression would be
attributable:

```
UNDEFINED CITATIONS: NONE
UNDEFINED REFS / CONTROL SEQ: NONE
OVERFULL HBOX: 0 total, 0 over 2pt
pages: 140
LaTeX ERRORS: NONE
```

Matches the stated 140 / 0 / 0 baseline exactly.

---

## CHANGE 1 — AIW-211, verify only. PRESENT AND CONSISTENT.

Both sites were already mirrored. Verified by character-for-character comparison after
applying the documented md→tex conventions (not by eyeball):

| Site | md | tex | Result |
|---|---|---|---|
| Prose paragraph ("Mammals implement…") | line 767 | line 962 | MATCH, 1184 chars |
| Table row ("Graduated consciousness levels") | line 782 | line 982 | MATCH, both cells |

Both carry "Theory of mind is not a rung on that ladder", "\emph{world-model} operation",
and "What the species gradient records is a \emph{correlation}". Table row also correctly
translates `→` to `$\rightarrow$` and `— Section 7` to `--- Section~7`. **No action needed.**

---

## CHANGE 2 — AIW-161, §3.4.4. MIRRORED.

### Edit 2a — `paper.tex`, two new paragraphs

**Before** (paper.tex ~L467-469):

```latex
...and a conscious system (which constructs a lived temporality from the recursive echoes of its own self-modeling).

\textbf{Qualia as implicit model echoes.} The qualitative character...
```

**After** (paper.tex L467-471) — two paragraphs inserted at exactly the specified location,
between the weather-simulation paragraph and the qualia paragraph, inside the existing
`{\revstart ... \revend}` block that wraps all of §3.4.4:

```latex
...and a conscious system (which constructs a lived temporality from the recursive echoes of its own self-modeling).

\textbf{Constructed order and the cognitive arrow.} The claim extends from temporal texture to temporal \emph{order}: ... \citep{Kolers1976}. ... \citep{Geldard1972}. ... roughly 80~ms that follow it \citep{Eagleman2000}. And \citet{Libet1979} --- a distinct result from the volitional-timing work discussed in Section~4.2.2 --- ... \citet{Dennett1992} drew the moral ... ``entering consciousness'' --- their Stalinesque/Orwellian indeterminacy --- ... remembering is reconstruction \citep{Bartlett1932}, and episodic remembering and imagining draw on one constructive system \citep{Schacter2007,Hassabis2007} --- so ordered temporal content at every scale is simulation output.

What the construction does not supply is its own direction. ... so the cognitive arrow is \emph{inherited} from the thermodynamic one, not built alongside it. This is a containment claim, not a correspondence claim: ... The simulation constructs \emph{which} order its contents carry; the medium it is built in fixes which way order runs.

\textbf{Qualia as implicit model echoes.} The qualitative character...
```

Translation decisions applied: `**bold**`→`\textbf{}`, `*emph*`→`\emph{}`, `—`→`---`,
`"quotes"`→` ``…'' `, `80 ms`→`80~ms` (matches the file's existing `262~Hz`),
`Section 4.2.2`→`Section~4.2.2` (matches `Section~4.2`, `Section~3.5` elsewhere),
inline `(Author, Year)`→`\citep{}`, narrative `Author (Year)`→`\citet{}`.

### Edit 2b — `references.bib`, eight entries appended

Appended at end of file under a section comment matching the file's own convention
(`% --- AIW-92 §4.2.3 …` style):

```
% --- AIW-161 §3.4.4 additions: postdiction, constructed temporal order, constructive memory ---
```

| Key | Type | Notes |
|---|---|---|
| `Kolers1976` | @article | `von Gr{\"u}nau` — matches the file's existing `{\"u}` convention (`Br{\"u}ntrup`, `G{\"u}nt{\"u}rk{\"u}n`, `K{\"o}nig`). BibTeX parses the von-part correctly; renders "Michael von Grünau". |
| `Geldard1972` | @article | Title quotes written as `` ``rabbit'' ``; renders as curly quotes in the PDF. |
| `Eagleman2000` | @article | |
| `Libet1979` | @article | **Added alongside** `Libet1985`, which was left untouched. Both exist; both cited. |
| `Dennett1992` | @article | **Added alongside** the `Dennett1991` book entry, which was left untouched. Both exist; both cited. |
| `Bartlett1932` | **@book** | 1932 original, publisher Cambridge University Press, **no DOI field** (the 1995-reissue DOI deliberately omitted). |
| `Schacter2007` | @article | See journal-name note below. |
| `Hassabis2007` | @article | |

All eight use the file's aligned field style and bare (un-prefixed) DOIs, matching
neighbouring entries.

**One deviation from the metadata block I was handed, and why.** The brief gave Schacter's
journal as *Philosophical Transactions of the Royal Society B*. The `.md` reference list
(line 1502) writes *Philosophical Transactions of the Royal Society B: Biological Sciences*.
The `.bib` convention is to mirror the `.md`'s journal string verbatim — proven by
`Llinas1998`, whose bib journal field (line 667) matches the `.md`'s
"…Royal Society **of London** B" exactly rather than normalising it. Since the `.md` is the
declared authority and the difference is additive rather than contradictory, I used the
`.md` form. Flagging it because it departs from the "verbatim" instruction.

---

## CHANGE 3 — §4.2.2 uniform-lag / constructed-order clause. MIRRORED.

Landed mid-task via coordinator message. Mirrored into `paper.tex` L737, appended to the
same paragraph, immediately after "…with the same latency." and before "The subjective
experience of temporal coherence…".

**Before:**

```latex
The ESM does not backdate anything; it simply runs on a slower clock that receives all its inputs with the same latency. The subjective experience of temporal coherence is genuine \emph{within the simulation},
```

**After:**

```latex
The ESM does not backdate anything; it simply runs on a slower clock that receives all its inputs with the same latency. This is the default case, and it should not be confused with the constructed-order account of Section~3.4.4: uniform lag explains why within-simulation order \emph{usually} appears coherent without any revision, while the postdiction findings show what fixes order when arrival timing is ambiguous or actively misleading. The two are complementary --- no backdating is required to explain the readiness-potential offset, and no uniform lag can explain a percept that incorporates events occurring after the moment it is attributed to. The subjective experience of temporal coherence is genuine \emph{within the simulation},
```

No new citations; `references.bib` unaffected by this change, as stated.

---

## VERIFICATION 1 — Build

`.venv/bin/python scripts/build_full_pdf.py` (bibtex run with sandbox disabled). Final run:

```
UNDEFINED CITATIONS: NONE
UNDEFINED REFS / CONTROL SEQ: NONE
OVERFULL HBOX: 0 total, 0 over 2pt
pages: 142
LaTeX ERRORS: NONE
```

vs baseline 140 / 0 / 0. **Page count 140 → 142 is the added content, not a regression** —
three new paragraphs plus eight bibliography entries. Every quality metric held at zero.

Independent project gates also run, both green:
- `pytest scripts/test_content_integrity.py` — **32 passed**
- `scripts/verify_references.py --check` — **OK, 718 references (548 verified, 170
  verified-manual), all verified and unchanged; 343 printed bibtex citations matched.**

## VERIFICATION 2 — String-check of the built PDF

Extracted `tmp/build-full/paper.pdf` with PyMuPDF, de-hyphenated across line breaks
(`-\s*\n\s*` → ``), collapsed whitespace, then reduced to `[a-z]` only — which deletes the
interleaved page numbers entirely, so they cannot split a phrase. Searched short fragments.

All 9 required fragments **FOUND**: `Constructed order`, `cutaneous`, `postdiction`,
`Stalinesque`, `containment claim`, `inherited`, `default case`, `actively misleading`,
`complementary`.

The page-number hazard was real and observed: the raw stream contains
`…1976. doi: 10.1016/0042-6989(76)90192-9. **132** Mark A. Kramer…` — a page number injected
mid-bibliography. The normalisation above defeats it.

All 8 in-text citations verified as **rendered** in the PDF, not merely present in source:
`(Kolers and von Grünau, 1976)`, `(Geldard and Sherrick, 1972)`,
`(Eagleman and Sejnowski, 2000)`, `Libet et al. (1979)`, `Dennett and Kinsbourne (1992)`,
`(Bartlett, 1932)`, `(Schacter and Addis, 2007; Hassabis et al., 2007)`.

All 8 bibliography entries verified as rendered. The two hazard pairs render as distinct,
adjacent entries:

```
Benjamin Libet. Unconscious cerebral initiative and the role of conscious will in
voluntary action. Behavioral and Brain Sciences, 8(4):529–539, 1985.
Benjamin Libet, Elwood W. Wright, Bertram Feinstein, and Dennis K. Pearl. Subjective
referral of the timing for a conscious sensory experience: …

Daniel C. Dennett and Marcel Kinsbourne. Time and the observer: The where and when of
consciousness in the brain. Behavioral and Brain Sciences, 15(2):183–201, 1992.
```

## VERIFICATION 3 — Final md ↔ tex agreement, computed against the CURRENT md

Deliberately **not** based on the diff computed at task start — re-read from disk at the end,
locating each passage by content search rather than by remembered line number. The tex was
mechanically converted to md form (citation macros → their inline equivalents, `\textbf`/
`\emph` → `**`/`*`, `---` → `—`, `~` → space, `` `` ``/`''` → `"`), then compared for exact
string equality.

```
md lines 354/356/576  ->  tex lines 469/471/737

MATCH  AIW-161 3.4.4 para 1 (Constructed order)   2148 chars
MATCH  AIW-161 3.4.4 para 2 (cognitive arrow)      617 chars
MATCH  S308  4.2.2 added clause                    527 chars
MATCH  AIW-211 mammals paragraph                  1184 chars
MATCH  AIW-211 table row cell 1 / cell 2

Kolers1976 Geldard1972 Eagleman2000 Libet1979
Dennett1992 Bartlett1932 Schacter2007 Hassabis2007   bib=True tex=True  (all 8 OK)

preserved Libet1985    bib=True tex_citations=2  OK
preserved Dennett1991  bib=True tex_citations=4  OK

RESULT: ALL CHECKS PASS
```

Exact equality, not similarity — every sentence of every `.md` passage has an equivalent in
the `.tex`, with nothing added and nothing dropped.

**Extra guard against a fourth unnoticed edit:** `git --no-pager diff --no-color HEAD` (with
`--no-color`, since this repo sets `color.diff = always` and a coloured diff silently defeats
`^+` matching). Working tree is clean against HEAD; the parent session committed both `.md`
changes and my `.tex`/`.bib` edits as `45d1c9c3` and `c98a9e26` while I worked. Inspecting
those two commits confirms the `.md` gained exactly the three passages handled here plus
eight entries in its own human-readable reference list — all eight cross-checked
field-by-field against my `.bib` entries (authors, year, title, journal, volume, issue,
pages, DOI): all agree.

---

## FINDING — pre-existing defect, NOT introduced here, NOT fixed

In §4.2.2, the same paragraph Change 3 extends, the `.tex` has lost a possessive that the
`.md` has:

- **md** (L576): `…has a direct bearing on Libet's (1985) finding that the readiness potential…`
- **tex** (L737): `…has a direct bearing on \citet{Libet1985} finding that the readiness potential…`
- **PDF renders**: `…has a direct bearing on Libet (1985) finding that the readiness potential…`

`\citet` renders "Libet (1985)", so the published sentence is ungrammatical. This predates
this task and is outside the three changes I was asked to mirror, so I did not touch it.
The fix is one edit, and the file already contains the idiom it needs — nine words later
the same paragraph correctly uses
`\citeauthor{Schurger2012}\textquotesingle s (2012) accumulator model`:

```latex
a direct bearing on \citeauthor{Libet1985}\textquotesingle s (1985) finding
```

Recommend applying it on the next `.tex` pass.

## Not mirrored / could not mirror

Nothing. All three changes are fully mirrored.
