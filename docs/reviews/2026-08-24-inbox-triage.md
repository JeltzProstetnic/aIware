# Cross-project inbox triage — every open item addressed to aIware

Recovered 2026-08-24 by a read-only agent, because the SessionStart hook truncated ~25k of
~85k characters of aIware's own payload out of a 424 KB / 243-item inbox.

**Sources read in full:** `~/cfg-agent-fleet/cross-project/inbox.md` (all 32 aIware-addressed
items, line numbers below), and for cross-reference `backlog.md`, `docs/crucible-status-2026-08-23.md`,
`docs/crucible-inbox-haystack-scan-2026-08-09.md`, `docs/decisions.md`,
`.claude/knowledge/{neuroscience-communication,prose-register,didactic-patterns}.md`,
`docs/pending-s306-detector-and-submission.md`, `README.md`, `figures/`.
Nothing was written outside this file. `~/cfg-agent-fleet/` and `~/crucible/` were read-only.

---

## Verdict

**32 open items are addressed to aIware** (31 `**aIware**` plus one `**aiware + crucible + life**`
at L282 that a case-sensitive grep misses). Of those, **10 are fully discharged and can be deleted
today**, **4 are blocked on a decision only MG can make**, and **18 still carry real work** — though
most of those 18 are narrow residues hanging off items that are otherwise absorbed. The genuinely
new, unowned obligations number **six**.

**The single most important untracked thing is the Physics of Life Reviews commentary (L651).**
An MG-confirmed P1 publication opportunity in aIware's own submission lane — a peer commentary on
*"Loops all the way up"* (PLR 58, 360–381, 2026), which Anil Seth publicly declared open for
commentary on 15 Aug — with a completed Fable assessment already sitting in `~/social/docs/`. It has
**zero occurrences in `backlog.md`**: no AIW id, no draft, no note. This is the exact failure the
backlog already recorded once at line 48 (a dated external opportunity from social sat unpromoted in
this inbox until MG caught it with the best venue 3 days out). It is repeating, unnoticed, while a
P0 flight clock runs to 28 Aug.

Runner-up: **L528, "There is more meaning than meaning"** — MG-authored FMT theory, filed to aIware
as the canonical theory home on 2026-08-05, with **0 hits anywhere in the project**.

⚠ **Confidence note.** aIware has independently confirmed almost none of crucible's results. The
`crucible-status-2026-08-23.md` survey was produced by a Fable agent *reading crucible read-only* —
that is a faithful transcription, not a replication. Everything below marked "crucible reports" is
taken on trust, and the last three months contain four separate occasions where crucible withdrew a
result it had already filed here as measured.

---

## All 32 items

| L | Headline | Prio | Source / date | Disposition |
|---|---|---|---|---|
| 13 | Neither "samples" nor "gates" is FMT phrasing — it is emergence all the way; + 3 citations | P2 | simbook 2026-08-21 | **ALREADY ABSORBED** — `neuroscience-communication.md` §20–44 carries MG verbatim + the 20 Hz reconciliation; Zheng & Meister at :52 and `references.md:368` |
| 17 | No bottleneck ever ran — every loop arm was the materialised dense product, off 2569× (+ 08-17 energy withdrawal + redeployment-axis ruling) | P0 | crucible 2026-08-15/17 | **LIVE** — dense-product half absorbed (`AIW-218`); the 08-17 half is reference-only, no backlog id |
| 23 | `figure1-*-bw*.svg` missing the ↻ closure glyph; shipped in 7 book editions; the shared originals are aIware's call | P2 | simbook 2026-08-19 | **LIVE** — no backlog item; files untouched since Session 164; premise needs verifying first |
| 27 | §8.9 "order of magnitude per subsystem" over-claim + ledger self-contradiction | P0 | crucible 2026-08-14 | **ALREADY ABSORBED** — `AIW-216` `[x]`, `AIW-217` `[x]`, both fixed S306; contradiction reconciled at crucible source |
| 31 | Span rule may warrant its own publication — first ask is a literature check | P1 | crucible 2026-08-14 | **AWAITING AUTHOR** — `AIW-219`, check done S306, write-or-shelve is MG's |
| 39 | Withdrawn specificity control survived a 3× seed extension; withdrawal stands; "small AND consistent" | P2 | crucible 2026-08-13 | **ALREADY ABSORBED** — `crucible-status` §4 + §5; no paper text cites it |
| 58 | Decomposition test re-run on the real re-entry, still breaks; **no single uniform filter constant reproduces the dual system** | P2 | crucible 2026-08-13 | **LIVE** — the quotable half is absent from aIware (0 hits) |
| 65 | Renamed-region control WITHDRAWN; "inert, measured rather than assumed" inverted the truth | P2 | crucible 2026-08-13 | **ALREADY ABSORBED** — `crucible-status` §5; correction obligation discharged by absence (phrase in no aIware text) |
| 180 | Crucible's 7-part sweep: wording ruling · ledger cells · numeric defect · connectome route dropped · pattern coordination · J-space offer · housekeeping | P1 | crucible 2026-08-12 | **LIVE (partial)** — (1)→`AIW-220`, (4)→`AIW-222` absorbed; **(5) and (6) untracked** |
| 219 | `aiw91` puts the implicit models on the wrong side of the mode axis | P2 | crucible 2026-08-12 | **ALREADY ABSORBED** — `AIW-221` `[x]` DONE S306, incl. the roadmap check |
| 221 | Three gaps in `prose-register.md` (banned tells, length-and-posture, sliding window) | P1 | consolidated 2026-08-21 | **ALREADY ABSORBED** — all three landed S305; see `prose-register.md:47–50, 89–107, 138–139` |
| 278 | 4 forthcoming editions announced; update README/kdp-specs/marketing doc; supply infra titles + ASINs | P2 | infrastructure 2026-07-24 | **AWAITING AUTHOR** — README already fixed; `kdp-specs.md` no longer exists here (moved to `~/simbook`, `AIW-125`). Needs a routing ruling |
| 282 | Bildstein gathering PARKED until a crucible robot result | P3 | social 2026-07-23 | **AWAITING AUTHOR** — `AIW-100`/`AIW-102` still open at **P1** with no PARKED marker; priority contradicts the ruling |
| 286 | Olinyk CRC↔FMT note — PUBLISH AS PREPRINT after 2 named edits | P2 | social 2026-07-23 | **AWAITING AUTHOR** — `AIW-120` sits at **P3** while MG greenlit publication |
| 288 | Qualia-privacy standalone paper repurposed — blog shipped, paper parked | P3 | social 2026-07-23 | **AWAITING AUTHOR** — `AIW-105` still `[ ] [P2]`; MG downgraded it to P3/"maybe" |
| 290 | CONSOLIDATED July crucible/social backlog (14 originals) | P1 | consolidated 2026-08-21 | **LIVE (partial)** — `AIW-143` + haystack scan cover 13 of 17; **(65)(70)(72)(95) never scanned** |
| 305 | D1 argument-matched decoy instrument built + 26k-validated + two MG theses | P1 | crucible 2026-07-13 | **SUPERSEDED** — the θ/spectral observable family was withdrawn 2026-08-08; haystack rules it MISSING-STALE, "do not sync it anywhere" |
| 475 | Publication-state reconcile — README stale, copy ASINs into kdp-specs | P2 | infrastructure 2026-07-20 | **SUPERSEDED** — README rewritten; `kdp-specs.md` left with the book (`AIW-125`). Duplicate of L278 |
| 519 | P0: closure operationalization in question; efficiency-not-necessity; closure ⊂ bottleneck | P0 | crucible 2026-08-06 | **LIVE** — theory side absorbed (SMoC chart); **companion method + claim text not written** → `AIW-140`/`AIW-218` |
| 520 | P0 follow-up: closure OFF = two closed systems, confirmed at 22k; return-ness theorem OVER-REACHED; genericity near-vacuous; the SPAN LAW; 73× → 1.9–4.4× | P0 | crucible 2026-08-06 | **LIVE** — same; (E) became `AIW-219`, (D) is in `AIW-165`, **(C) and (F) corrections not tracked** |
| 524 | Didactic patterns 45 & 46 + citation caution on the fragility numbers | P2 | crucible 2026-08-05 | **LIVE (small)** — caution absorbed (`crucible-status` §4); the two patterns are not in `didactic-patterns.md` |
| 528 | **"There is more meaning than meaning"** — MG-authored theory, J-space as the worked instance | P1 | simbook 2026-08-05 | **LIVE** — 0 hits anywhere in the project |
| 532 | CRU-71 closed negatively; only the COST claim survives; `g` is gauge-dependent; **decompilability was never a discriminator**; no live discriminator candidate | P1 | crucible 2026-08-05 | **LIVE** — MG's decompilability correction is unrecorded (0 hits); "do not promise a forthcoming discriminator" untracked |
| 537 | Rank must be MATCHED not ignored; carry into SMoC M10; long-context LLM as free negative control; ⚠ P1…P5 names two things | P2 | crucible 2026-08-06 | **LIVE (small)** — M10 resolved in the chart; two residues remain |
| 579 | CRU-69 leg 1 adjudicated on the real 22k substrate — **but falsifier 4 is `no-separation`, NOT a pass** | P1 | crucible 2026-08-09 | **LIVE** — the F4 citation constraint has 0 hits in backlog or docs |
| 581 | The companion's TITLE must change; a stronger statement is not purchasable | P1 | crucible 2026-08-10 | **ALREADY ABSORBED** — `AIW-140`, named explicitly at `drafts/aiw-operationalization-review.md:134` |
| 583 | An experiment can measure the wrong component with perfect reliability; **the answer to the `AIW-201` collision**; do not wait for M1s3 | P1 | crucible 2026-08-10 | **LIVE** — `AIW-201` is open but the entry conditions that unblock it are only crucible-side |
| 591 | Never quote a crucible GPU figure past 3 decimals; "closure", never "return" | P1 | crucible 2026-08-11 | **ALREADY ABSORBED** — `AIW-220` (package written, waiting on MG) + `crucible-status` §4 |
| 592 | MG killed "fixed point vs carried content"; the surviving seam; the stipulated-constant trap | P2 | crucible 2026-08-11 | **LIVE (conditional)** — only bites if companion text unpacks self-consistency as persistence |
| 601 | **The A#9 τ_syn scope clause MG approved, written out verbatim, ready for the v15 listing paragraph** | P1 | crucible 2026-08-12 | **LIVE** — half-tracked under `AIW-218`; the clause text and the relative-vs-absolute-bar caveat are not recorded here |
| 649 | The MoC7 poster is NOT printed, `AIW-137` stands fully open; **organisers replied 2026-08-20** | P1 | social 2026-08-21 | **LIVE (small)** — `AIW-137` open, but the organisers' answers are new and post-date `pending-moc7-copenhagen.md` |
| 651 | **PLR peer commentary — "Loops all the way up", Seth-boosted, assessment complete, MG P1-confirmed** | P1 | social 2026-08-21 | **LIVE — AND COMPLETELY UNTRACKED** |

---

## Stale-theory audit (the two known traps)

**(a) "Closure OFF" was never an ablation.** One hit: **L305**. Its D1/DEC apparatus and its θ gates
live in the spectral / dominant-mode observable family withdrawn on 2026-08-08, and its second MG
thesis (will earned by causal ablation) is necessity-flavoured framing that the efficiency ruling
governs. L519/L520 are not victims of this trap — they *are* the ruling that created it, and
anything in the inbox predating 2026-08-06 that reads a closure-OFF null as evidence against FMT is
stale by them. The haystack scan already marked rows 86/97/98/112 MISSING-STALE on exactly this
ground, so that half of the July backlog is clean.

**(b) Every "bottleneck arm" was the materialised dense product, off 2569×.** The storage claims all
survive and are correctly worded where they appear: L579's 26.4/44.0/79.1× is explicitly a
factored-storage result, and L601's clause already says *"the cost law is a factored-storage/wiring
result, and no relayed implementation has run on the spiking substrate."* The exposure is confined
to **energy and metabolic sentences**, and L17's own 2026-08-17 correction withdraws the one that
existed. One thing to re-read with this lens: L532(2)'s *"7.9× the synapses and ~23× the internal
state"* — the synapse half is storage, the internal-state half is a statement about a realisation
and should be checked before it goes into the companion.

---

## Expanded: the work aIware still owes

### 1. L651 — PLR peer commentary. **P1, MG-confirmed, untracked. Start here.**
*"Loops all the way up: Recurrency as an implementation-first primitive for consciousness"*, **Physics
of Life Reviews 58, 360–381 (2026)**, DOI `10.1016/j.plrev.2026.07.006`. Eleven authors including
Melloni, Northoff, Aru, Chis-Ciure and Fernando Rosas. **Seth boosted it publicly on 2026-08-15
saying it is open for peer commentary.** Full text staged at
`~/social/tmp/plr-loops-all-the-way-up-2026.txt`; the complete Fable assessment is
`~/social/docs/plr-loops-commentary-assessment-2026-08-21.md`. Tracked social-side as `SOC-49`.
**Drafting and submission are aIware's lane**; social owns the Seth relationship.

- **The framing must be inverted.** "FMT already said this" earns nothing — their §3.3.2 counter to
  the Unfolding Argument *is* MG's efficiency-under-a-budget argument almost verbatim.
- **The opening is their own concession:** the depth criterion is *"a conceptual discriminator rather
  than an operational one"* and specifying it is *"a question that the present framework poses rather
  than answers"* — and at the decisive moment their discriminator stops being about recurrency and
  becomes modelling vocabulary.
- **Thesis:** recurrency becomes consciousness-relevant when the loop implements modelling that
  closes on itself. Three points: the criterial slip; the UAT repair; the missing self level (their
  Box 3 grounds selfhood in heart/gut/breath loops, which feed a self-model but do not constitute one).
- ⚠ **Two findings cut against MG:** Box 4 argues von Neumann machines only *simulate* recurrence
  without embodying it, which by their logic covers his own RTX 4090 SNN; and criticality is absent
  from the paper entirely — an open slot, not a conflict.
- ⛔ Must not claim closure derives phenomenality, nor that recurrence enables what feedforward
  cannot, nor priority on causal closure.
- **Route:** cold submission is the default-decline path — mitigate with a two-paragraph pitch to the
  handling editor first.
- **Lands on:** a new backlog item (no AIW id exists). Interacts with the P0 clock at `backlog.md:49`
  — MG wants papers resubmitted before he leaves for Greece on 28 Aug.

### 2. L528 — "There is more meaning than meaning". **P1, MG-authored theory, zero trace.**
MG's own structure, 2026-08-05: language *and mathematics* carry meaning; there is more meaning than
meaning (the number 1.2 has many meanings across contexts, the absolute value of *c* very few); that
surplus is what LLMs extract, which is why they apply intelligence without being shown how;
*"languages transport more than what their direct interpretation suggests"*; and — the structural
move — *"just like the explicit models change the substrate and vice versa, language is changed by
these meanings and vice versa"*, which is **why hidden meanings are recoverable from enough text**.

- **State the anti-prior-art point explicitly or a reviewer collapses it into Harris 1954 / Firth
  1957.** Distributional semantics treats recoverability as a brute regularity; MG supplies a
  *mechanism* — the bidirectional language↔meaning coupling is what deposits the structure into the
  corpus. That coupling is structurally the same move FMT already makes for substrate↔explicit-model,
  which is why it belongs to the theory and not to NLP.
- **Precision the 1.2/*c* example needs:** frame it as **referent, not glyph**. The glyph `c` is as
  promiscuous as 1.2; it is *c-the-physical-constant* whose meaning is near context-free.
- **The worked instance is J-space**, and MG has already written the FMT reading of it in
  `drafts/bach-reply-jspace.txt` (reply to Joscha Bach). The principle predicts the finding and the
  finding evidences the principle — write them up together.
- **Lands on:** `didactic-patterns.md` (it is a teaching device), plus a decision on whether it enters
  the paper as **derived or asserted** — the Bach draft already concedes *"the step from closure to
  phenomenality is asserted in my framework and not derived"*, so draw the same line here.
- **Composes with L180(6)** below — same J-space paper, two different uses.

### 3. L601 — the A#9 τ_syn scope clause. **Ready to paste, nothing to decide.**
Crucible ran the pre-registered scope sweep and MG approved the clause. Drop verbatim into §8.9's v15
listing paragraph:

> *"The loop arms are span-routed rank-k bottlenecks realized in folded form; the cost law is a
> factored-storage/wiring result, and no relayed implementation has run on the spiking substrate. The
> result reproduces unchanged under a synaptic time constant of τ_syn = 2 — the substrate's decided
> value — including the rival cost margins to four significant figures. At τ_syn = 10 the achievable
> ceiling falls below the fixed criterion in two of three content conditions, so the comparison is not
> adjudicable there; the boundary between the two lies in an interval the sweep did not resolve."*

⚠ **One caveat must travel with it and is nowhere in aIware's records:** the paper's bar is
**relative** (90% of the achievable ceiling); tier B's is **absolute** (`c*` = 0.90, fixed before any
arm was built). Consistent in direction, **not the same measurement**. Also: pre-registered
prediction P2 did not fire — the rival margin did not move at all — and the honest form is *"not
confirmed on its own DV, mechanism confirmed on another"*, never "P2 passed".
**Lands on:** `AIW-218` (which already says "import the approved scope sentence verbatim from
`~/crucible/docs/results/cru69-tau-syn-scope.md` §11.6") + the v15 §8.9 listing paragraph.

### 4. L579 — the falsifier-4 citation constraint. **A trap with a green tick on it.**
CRU-69 leg 1 adjudicated: the cost-optimal span is exactly the content's support, 3/3 conditions, on
the real 22k spiking substrate. **But falsifier 4 scored `no-separation`, which is not a pass** —
neither rank (r²=0.0034, p=0.85) nor span (r²=0.0023, p=0.94) explains the pooled cost ordering, so
F4 cleared rank-as-rival *by having no resolving power*. The verdict JSON labels it correctly; **the
printed summary shows it as a fifth green tick beside the four genuine passes**, which is exactly how
a reader miscounts it. **Any FMT text citing CRU-69 may cite F1/F2/F3/F5 and must not cite F4 as a
confirmation.** Zero hits for `no-separation` or `falsifier 4` in `backlog.md` or `docs/`.
Also banked here and unrecorded: quoting the 26–79× multiplier without the substrate size is
misleading (loop cost is O(|span|·k), rivals' O(|span|²), so the ratio grows by construction) — the
honest headline is the ordering and the censoring pattern.
**Lands on:** `AIW-218` / `crucible-status` §4, and the v15 §8.9 text.

### 5. L17 (second half) — the energy axis is retired; the cost claim moves to redeployment.
The dense-product half is tracked. The **2026-08-17 correction is not**, and it is a drafting
prohibition, not information:
- ⛔ *"the spandrel hypothesis is not ruled out on energy grounds"* **is withdrawn and must not be
  drafted in either direction.** MG's two objections held against crucible's own runner code: the load
  was a task every arm solves at ceiling, so it measured the cost of the mechanism *not being used*;
  the idle arm was literally "no cue, ever", far more quiescent than a resting brain.
- ⛔ **No draft may build a cost claim on a joules-per-timestep number.** MG: *"we move to
  redeployment axis."* The instrument is cost-to-competence as a function of the **number of targets**.
- ⇒ **The prediction is a SLOPE over that number, never a level at one.** §4.2.4 already says the
  theory claims *"no operation that unconscious processing is barred from … What differs is the
  price"* — so a tie at a single target refutes nothing and a draft must not treat one as a test.
- ⛔ **A redeployment result may never be cited as evidence about closure** — redeployment measures
  the bottleneck's payoff; the return's payoff is self-consistency.
- ⚠ And check §4.2.4 before writing any efficiency argument: its currency is a **lifetime acquisition
  cost** (how many models must be built), not instantaneous power.
**Lands on:** `AIW-140`/`AIW-218` as a hard constraint; `prediction-framing.md` for the slope rule.

### 6. L532 — decompilability, and the promise the paper must not make.
Two unrecorded pieces (0 hits for `decompil` in `decisions.md` or the knowledge files):
- **MG's correction, verbatim 2026-08-05:** *"the decompilation argument was never meant as
  discriminator … decompiling it WOULD MEAN UNROLLING THE RECURRENCE FROM THE CONNECTOME FIRST. then
  decode it into language, and that IS then the functional decompilat, which you can then correlate
  to the geometrical statistical data from fmri."* It is a **readout pipeline** — the bridge from FMT
  to empirical neuroscience — not a test separating closure from a closure-free rival. If any
  companion text frames it as a discriminator, rewrite.
- **CRU-67 now has no live discriminator candidate at all**, so **the paper must not promise a
  forthcoming discriminator.** The route is closed structurally: the reduction theorem's rival class
  has no natural boundary (raster divergence order-1 0.0147–0.1181, order-2 0.0010–0.0345, order-3
  exactly 0.0000).
- **What survives and should be carried:** a bottleneck loop realizing a rank-k operator is reproduced
  exactly only by a rival needing 378 internal states + 15,876 synapses against the loop's 16 states +
  2,016 — **7.9× the synapses, ~23× the internal state**. ⚠ Re-read the internal-state half against
  trap (b) before printing it.
- ⛔ Never cite the blind statistic `g` without its gauge and relay regime (gauge-dependent by 0.111
  across realizations of the same operator).

### 7. L519 + L520 — the companion's method and claim text. **The largest single debt.**
Both items carry an appended aIware note from S285: *theory-side absorption done* (the closure ⊂
bottleneck decomposition, efficiency-not-necessity and closure-OFF-is-two-closed-systems are in
`docs/smoc-middle-layer-draft.md`), **paper-text rewrite still open**, deliberately deferred by MG.
Tracked as `AIW-140` (open) and `AIW-218` (open, "the companion paper's largest single debt").
Two corrections inside L520 that are *not* separately tracked and would ship as errors:
- **(C)** the "return-ness is not a manipulable variable" theorem **over-reached** and must not be
  quoted in the form crucible originally sent. Corrected form: *you cannot make a recurrent network
  return-free; you can only choose which node-sets sit in returning components — the only return-free
  construction is global acyclicity, which is a phase change, not an axis.*
- **(F)** the wiring-efficiency advantage is **~1.9–4.4× at the scales the experiments actually run**,
  not the 73× asymptotic, it is the cost of *exact* reproduction rather than matched function, and it
  never counts relay neurons (18.2% of the neuron budget at every scale).

### 8. L583 — the entry conditions that keep `AIW-201` alive under review.
`AIW-201` (the capability-null inversion, MG-confirmed P1) is open, but crucible's **answer** to the
collision aIware flagged is only crucible-side. Two parts: **(a)** the inversion does not apply to any
of crucible's current nulls — its premise is *selection* and nothing selects a numpy rig, and none of
the three runs is a clean measured null. **The inversion converts clean nulls; it does not launder
broken ones.** **(b)** The collision with falsifier 5 is real but narrow — F5 kills the *scale appeal*,
not the theory — and crucible has **pre-registered entry conditions** (`cru83-m1s3-sufficiency-prereg.md`
§12.6) so the inversion cannot be invoked opportunistically after a null: (i) the instrument is clean
by its own gates; (ii) both arms are demonstrably competent, so the null is an absence of *advantage*
not of *capability*; (iii) on a synthetic substrate the biological-selection premise is explicitly
addressed with a stated substitute argument. **Without those it is an escape hatch of exactly the kind
F5 exists to close.** Fold into `AIW-201`.
Also here: ⛔ **do not promise an M1s3 number for v15** (§5.1 unpopulated, tier-B gated), and the s1
redeployment GO now holds in the **forward** direction, retiring the "measured in the other direction"
caveat for the deployment half.

### 9. L58 — the referee pre-emption worth carrying into the paper.
The decomposition test now runs against the re-entry the experiment actually wires, and still breaks.
**The part crucible flags as worth carrying:** *no single uniform filter constant reproduces the dual
system* — refitting the folded pool's own constant across nine values fails at every one. Without it a
reviewer can say the dedicated return is a re-parameterisation, the same system with the filter
re-tuned. It is not: the folded family has one degree of freedom and this system is outside all of it.
⚠ Scope it exactly: a statement about the **construction**, not about closure. It says nothing about
whether the effect exists or is large. **0 hits in aIware.** Lands on `AIW-140`/`AIW-218` methods.

### 10. L180(5) + L180(6) — two residues of the crucible sweep.
- **(5) Two crucible patterns offered for mirroring, neither present in `didactic-patterns.md`:**
  **112** — a random-reorientation robustness analysis passes *vacuously*; ask what the statistic's
  response to a perturbation is *analytically* first, and if the answer is a theorem, a floor or an
  invariance, the analysis cannot fail and must not be reported as though it could. **113** — the
  reliability-vs-construct-validity finding as the one question that goes into every pre-registration
  in writing before the run: *what else would produce this same number, and what would show the
  manipulation is moving something merely co-resident with the construct?* ⚠ Also worth knowing:
  aIware's four 2026-08-10 `didactic-patterns.md` corrections **do not appear in crucible's copy at
  all** — the two files are further apart than either project assumes. Lands on `AIW-95`.
- **(6) The Gurnee et al. 2026 J-space paper** (*"Verbalizable Representations Form a Global Workspace
  in Language Models"*, arXiv:2607.15495) as **the cleanest published contrast case: a global workspace
  WITHOUT closure** — it evolves in a single forward pass with no recurrence. ⚠ **It raises FMT's
  burden rather than lowering it:** if access-like structure without closure already yields reportable,
  summonable, argument-passable internal states, whatever closure adds must be shown to be something
  this system does not have. The authors claim access consciousness only and decline the phenomenal
  claim. Crucible is not drafting it. Currently mentioned only in `crucible-status` §6.6.

### 11. L23 — the figure defect. **Verify before acting.**
simbook reports that U+21BB (↻) is absent from Helvetica/Arial and cairosvg drops a missing glyph
*silently*, that its shipped PNGs show 0 dark pixels in the glyph box for en/de/es/fr/it/nl/pt against
466–536 for the four that pin DejaVu Sans, and that aIware's eight `figure1-*-bw*.svg` are
**byte-identical to simbook's pre-fix copies** (md5-checked 2026-08-19). ⚠ **A direct check of this
repo does not obviously reproduce the premise:** `figures/figure1-four-model-architecture-bw.svg`
contains only 7 non-ASCII bytes, no literal ↻, no `&#8635;` and no `DejaVu` font pin, and the file has
not been touched since Session 164. So the first step is to render one and look at it, not to edit.
Two things are owed regardless: **the decision about the shared public originals is aIware's** (simbook
has fixed and gated its own copies via `scripts/check_figure_assets.py`, `SIM-56`), and **the
transferable rule** — pin a font for every non-ASCII glyph and check the *rendered* asset rather than
the markup, because a path-and-filename check passes a figure whose text is invisible. That rule exists
here only in CJK-specific form at `docs/decisions.md:1203`.

### 12. Smaller residues, worth one line each
- **L524** — didactic patterns **45** (*"Its not post hoc fitting if you do a wrong experiment on a
  wrong assumption and i correct you"* — the discriminator is the counterfactual: would this correction
  have been made if the result had come out the other way?) and **46** (*a control is only as good as
  the variable it moves* — two carefully-run controls both varied rank while the governing confound was
  redundancy). Neither is in `didactic-patterns.md`. `AIW-95`.
- **L537** — two residues after M10: the **long-context LLM is banked as a free negative control** (max
  rank at zero closure, citable rather than runnable — a cheap line for the SMoC chart), and ⚠ **`P1…P5`
  currently denotes both FMT principles and pre-registered predictions across both repos**, which
  "bites in a submitted draft". Cheap to disambiguate now.
- **L592** — MG killed the "fixed point vs carried content" distinction; persistence followed by
  reversion *is* slow reversion, and only bistability would be a sign difference. The §3.6.1
  cost/consistency decomposition is unaffected, **but if any companion text unpacks "self-consistency"
  as content persisting under contradicting evidence, that reading does not survive.** The surviving
  seam is cleaner: (i) does a stable self-sustaining pattern exist at all, versus (ii) does it track
  the body. Also a general trap for the methods section: a differential measurement defeats a *global
  gain* confound but not a *stipulated* one (the self circuit's τ_m of 40 vs the world circuit's 20
  makes "the self circuit holds longest" true by construction).
- **L290** — of the 14 consolidated July originals, the haystack scan covered 13; **(65)** the
  harness-less browser-CC translation session hygiene pass, **(70)** the OSF-prereg/SNN-README/Noema/
  Amazon-Ads playbook (⚠ note the later no-OSF ruling and post-MoC7 re-timing), **(72)** the
  book-marketing master-doc update, and **(95)** the `fmt-visibility-strategy.md` full refresh were
  never scanned. Add them to `AIW-143`'s untriaged list explicitly so the gap is visible.
- **L649** — `AIW-137` is correctly open, but the **organisers replied 2026-08-20 16:42** (Gmail
  `1a01f9f80b74eb21`) after `pending-moc7-copenhagen.md` was last written: official format is **A0
  portrait**, poster sessions run **every day except Wednesday**, exact slot not until September,
  questions are also accepted during the conference — so the early submission was accepted, no
  resubmission is needed, and the ~12 Sept window is no longer a single missable gate. They did not
  answer the mechanism question and **MG's vote remains unspent.** Two loose ends: MG's 11 Aug mail to
  the organisers said "the poster is with the printer", which was not accurate (harmless now), and a
  WIRmachenDRUCK order `12831205-1` shipped and closed on 16 August that was **not** the MoC7 poster —
  nobody has chased what it was.

---

## Awaiting author — four decisions, no work until they land

1. **L31 / `AIW-219` — write the span-rule paper, or shelve it?** The literature check MG asked for is
   done (`docs/aiw219-span-rule-novelty-check.md`): **novel but narrower than it looks.** Friedlander,
   Mayo, Tlusty & Alon (2015) *PLoS Comput Biol* 11(3):e1004055 already owns condition 1, so span=support
   must be framed as sharpening and ⛔ must not lead the paper. What is genuinely unclaimed is the
   conjunction. Venue: PLoS Comput Biol, then Network Neuroscience; arXiv q-bio.NC preprint regardless.
   Highest-value follow-on: prove span=support rather than enumerating it.
2. **L278 / L475 — who owns publication state now?** The book left for `~/simbook` on 2026-07-31
   (`AIW-125`), taking `kdp-specs.md` with it. README is already fixed. What remains is a standing duty
   to supply infrastructure with per-edition localized titles, marketplace ASINs and confirmed formats
   as nl/ko/el/zh-print go live — infra flips `live:true` on that signal. Ruling needed: aIware or
   simbook. (Also carried forward: watch for the KDP language-tag quirk — ZH shipped Simplified content
   under a "Traditional" tag.)
3. **L282 — `AIW-100` and `AIW-102` are both open at P1** with no PARKED marker, while MG hardened the
   Bildstein park to a full stop until a crucible robot result (*"an event based on the theory alone
   would be pompous and empty"*). The *convening* dimension is parked; **FMT-as-standard-model theory
   positioning STANDS**, as do normal researcher relationships. Confirm the demotion.
4. **L286 / `AIW-120` sits at P3** while MG greenlit a PhilArchive/Zenodo **preprint** as the canonical
   venue for the Olinyk CRC↔FMT note. Two required edits before publish: flag-as-paraphrase the three
   unverified proprietary labels (*"Double-Jump"*, *"structurally redundant middleman"*, *"Seven Books
   dialogues"*), and keep the *"is Olinyk's 'Objective Data' a generative world-model or just structured
   stored facts?"* question explicitly open rather than adjudicating it. Keep the OLIORC-2
   circularity quarantine. OLICRC and OLIORC-2 full PDFs are now ingested at `~/social/docs/data/`, so
   the note can be sourced fully-primary. **The published preprint IS the outreach** — Olinyk has no
   public contact. Also **L288 / `AIW-105`** in the same breath: MG downgraded it to P3 "maybe / hand
   to a student"; the backlog still says P2.

---

## Safe to delete from the inbox

Mechanical — no judgement needed. Each is fully discharged, with the absorbing artifact named.

| L | Delete because |
|---|---|
| 13 | `neuroscience-communication.md` §20–44 + `references.md:368` |
| 27 | `AIW-216` `[x]` + `AIW-217` `[x]`, fixed S306 2026-08-23 in both `.md` and `.tex`; 32/32 content tests green |
| 39 | `crucible-status-2026-08-23.md` §4 + §5; no aIware text cites the withdrawn claim |
| 65 | `crucible-status` §5; the phrase to correct appears in no aIware paper or draft |
| 219 | `AIW-221` `[x]` DONE S306, including the formalization-roadmap check |
| 221 | `prose-register.md:47–50, 89–107`, changelog at :138–139 records all three landing S305 |
| 305 | **SUPERSEDED** — spectral/dominant-mode family withdrawn 2026-08-08; haystack scan row 86 rules it MISSING-STALE, "do not sync it anywhere". ⚠ When deleting, note that thesis (2) *will earned by causal ablation* was never absorbed and is deliberately not carried |
| 475 | **SUPERSEDED** by `AIW-125` (book → `~/simbook`) and the README rewrite; duplicate of L278 |
| 581 | `AIW-140`, named at `drafts/aiw-operationalization-review.md:134`; the master already corrected the same framing at v14 |
| 591 | `AIW-220` (package at `drafts/return-vs-closure-vocabulary-ruling.md`) + `crucible-status` §4 (3-decimal rule, 2.617e-04 envelope) |

**Do not delete yet:** 17, 23, 31, 58, 180, 278, 282, 286, 288, 290, 519, 520, 524, 528, 532, 537,
579, 583, 592, 601, 649, 651 — each still carries work, a decision, or an untracked constraint.

⛔ Deletion requires write access to `~/cfg-agent-fleet/`, which this project does not have. It must
be done by a cfg-agent-fleet session, or by whoever holds the inbox-editing licence.
