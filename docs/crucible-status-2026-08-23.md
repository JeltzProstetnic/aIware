<!-- Action: reference -->
<!-- Tracked-by: AIW-124, AIW-216, AIW-217, AIW-218, AIW-219 -->
# Crucible status as of 2026-08-23 — what it means for the companion paper and the operationalization

**Surveyed S306 by a Fable agent reading `~/crucible` read-only.** Covers everything banked,
withdrawn or in flight since 2026-08-12, which is the last point aIware absorbed. Crucible's own
banked/hypothesis labelling is preserved — a caveat dropped here becomes an over-claim in print.

Crucible's position today: **M1s3of5, CRU-113 (contingency-mirror discrimination) is the critical
path.** Suite 1947 passed / 4 skipped / 0 failed. Nothing from CRU-113 is banked.

---

## 1. ⚠⚠ The three loudest things

**(a) "Bottleneck arm" never meant bottleneck.** `~/crucible/docs/results/cru108-closure-vs-wiring.md`
(2026-08-14): every loop arm ever run was the **materialised dense product** `A @ Vt` — billed
125,888 connections, actually **323,424,256**, off by **2,569×**. The span rule's **storage** claim
is untouched, because the factored bill is the right quantity for a storage claim. **Every
dynamical or metabolic sentence about a "bottleneck arm" describes a dense matrix** and must be
re-read that way before it appears in a draft.

**(b) The closure cost was the wiring, not the closure.** Crossing return × realisation, 208 cells,
8/8 individuals: **closure itself costs ×1.03 at rest and ×1.01 under load.** The previously banked
8.72× was the wiring realisation. This is the one clean energy number that survives.

**(c) The energy negative is WITHDRAWN as a general claim** (2026-08-17). MG's two objections held
against the runner code: the load was a task every arm solves at ceiling, so it measured the cost of
the mechanism *not being used*; the idle was "no cue, ever", far more quiescent than a resting
brain. ⛔ *"The spandrel hypothesis is not ruled out on energy grounds"* **must not be drafted in
either direction.** MG moved the cost claim to the **redeployment** axis — a **slope over number of
targets, never a level at one** — and ⛔ **a redeployment result may never be cited as evidence
about closure** (CRU-111, blocked on the substrate).

---

## 2. The operationalization — unchanged, and now sharper

The closure manipulation is still **efferent disconnection of the self region, not an ablation of
closure**. It is now *measured* as distinct from a second cut nobody had separated
(`~/crucible/docs/decisions.md`, "2026-08-12 night — RULING", answering `AIW-193(b)`; MG: *"no idea,
crucible is ground truth"*):

| manipulation | cost | leaves |
|---|---|---|
| efferent disconnection of the self region (closure OFF) | **14.03%** of synapses | **two** closed systems: {CE,EWM,ISM} and {ESM} |
| cutting the self region's own recurrence (ESM→ESM) | **6.23%** | **one** closed system |

Scope: structural, region granularity, this synthetic connectome family. **Dynamical survival is
unmeasured** (CRU-99 filed, unrun). Cross-checked independently: the cru83 partition puts the
re-entry block at **2,158,030 of 15,391,490 synapses = 14.0%**, matching CRU-58's graph search by a
different method.

Also new: **MG killed the blind-spot/dose manipulation** — *"what you made is just more closure"*.
Closure is constitutive, not a parameter. In CRU-113 the manipulation is the loop relays' return
into content against a capacity-matched bypass with no return; the loop arm deliberately has more
synapses overall, because the return **is** the manipulation.

---

## 3. What the companion paper (AIW-124) now owes

1. **WRITE — the biggest new obligation.** The evidence ledger's gap **inverted** on 2026-08-14: the
   master now lists eight unbroken ordinals including A#7/A#8/A#9, and **the companion explains none
   of the three architectural-cost rows.** Crucible tracks closing it as **CRU-85**; they supply
   substance and framing, aIware writes the prose. ⚠ **Cite the ordinal, never the page** — pages
   moved 104–105 → 107–109 between builds.
2. **REWRITE — methods framing.** The `[[CRUCIBLE:]]` method sections (CRU-94, rides AIW-124 at MoC7)
   must describe the banked loop arms as materialised dense products. The wired bottleneck is a
   separate, later object. Import the approved scope sentence verbatim from
   `~/crucible/docs/results/cru69-tau-syn-scope.md` §11.6.
3. **HOLD — any energy or metabolic section.** See §1(c).
4. **DO NOT CITE — the withdrawn set:** the 3.92 SD specificity claim; the renamed-region control as
   evidence; "two currencies disagree"; "~4× cheaper at rest"; the general energy negative; the
   180.7× margin at τ=5 (a ladder-granularity artifact — the criterion was crossed by 0.0177).

---

## 4. Numbers safe to quote today, caveats inseparable

- **Rival cost margins 26.4× / 44.0× / 79.1×** — folded rank-k realization, factored-storage result;
  reproduces exactly at τ_syn = 2 (the substrate's decided value) and at 5; **not adjudicable at
  τ = 10**, where the achievable ceiling falls to **0.8984** against a fixed c* = 0.90, short by
  0.0016; boundary in the interval **(5, 10]**. The rival family is non-monotone in its own budget in
  19% of cells (loop and control arms: 0%).
- **Subsystem scaling 195 → 2159 → 6048 synapses**, monotone at every size tested, unbounded at 126
  units (48 of 48 cells). ⛔ **Never "an order of magnitude per subsystem"** — 11.072× then 2.801×.
- **Closure OFF costs 14.03% dense / 12.0% modular, leaving 2 and 3 closed systems; return-freedom
  costs 74.0% / 71.5% (5.3× / 6.0×); self-recurrence cut 6.23%** — structural graph claims.
- **The return costs ×1.03 at rest, ×1.01 under load** (8/8, realisation held fixed).
- **Wired bottleneck: 575,488 vs 22,744,284 connections — 39.5× fewer at matched performance**;
  costs ~4 points of ceiling (0.954 vs 0.995); physical realisation costs exactly ×2 over the
  idealised bill. Its energy figures (×0.930 rest, ×0.532 load) are facts about **two operating
  points** and support no spandrel claim in either direction.
- **Reduction to practice:** 22,000 neurons / 15.4M synapses, completion 0.9924 closed vs 0.9923
  control, fold control **bit-identical (max diff 0.0)**, rebuild delta 6.2e-05 within the measured
  envelope 2.617e-04. **No capability claim** — both arms tie because the task is at ceiling.
- **Fragility under a synaptic filter:** lesioned loop arms 0.379–0.418 vs their exact folds
  0.927–0.979; blind-targeting gain +0.468/+0.480 vs +0.012/+0.046. Fragility is a **liability**,
  a claim about factorized vs unfactorized realizations of one operator.
- **Specificity: 23/24 at p = 1.5e-06 but only 0.09–0.18 pp.** Quotable only with both halves —
  **"small and consistent"**. The claim level stays withdrawn and the paired analysis was chosen
  after seeing the data.
- **FP envelope for any GPU figure: 2.617e-04.** The standing 3-decimal rule is consistent with
  everything above.

---

## 5. Withdrawn or corrected since 2026-08-12 — do not resurrect

- **Renamed-region control [WITHDRAWN 08-13].** MG: *"are you testing if variable names influence the
  result of the addition?"* With closure ON the region label is never read; both arms build the
  identical connectome; residuals 5e-9–1e-5 are GPU nondeterminism. ⚠ **Anywhere aIware mirrored
  "inert, measured rather than assumed", the truth is "guaranteed by construction; the measurement
  was a tautology."** It was re-run and re-reported a third time on 08-14 (48 of 152 cells wasted)
  before being deleted from the runner.
- **Self-region specificity [WITHDRAWN, then re-opened-not-re-established].** Equal-cut control gives
  1.78 SD against the pre-registered tie criterion. Durable instrument finding: **pattern completion
  cannot carry a specificity claim about the self region.**
- **Substrate re-founded 08-14 evening.** MG dismantled the relation/combining pathway (a dedicated
  binding mechanism contradicts §5.1). Code goes to **sparse graded 0..1**, Dale-clean by
  construction; scored task becomes tool use, headline DV **pause-before-acting slope** with a
  novelty interaction. ⛔ **Every completion figure banked under the bipolar code is void for the new
  substrate.**
- **CRU-115 visualization [BANKED but PAUSED by MG]** — wiring-grown 3-D anatomy recovers circuits at
  0.880 vs 0.273 chance; substrate is measurably a blob (Q = 0.196); scramble control clean
  (Q = 0.003). Do not build on it.
- **CRU-116 [OPEN BUG]** — intermittent full-suite segfault (exit 139), unreproduced. Crucible's own
  words: *"unexplained, not fixed."*

---

## 6. What crucible is waiting on from aIware

1. **§8.9 repairs** — D2 over-claim ✅ **fixed S306**; D1 under-claim **open** (the trailing scope
   sentence reads "one content condition" onto a span=support result measured in four). D3
   ("necessarily includes") is **not** an error — MG ruled, and the alternative wording is MG's pick.
2. **A clause rename** to *"removal of the self region's self-recurrence"* — *"ablation of the
   explicit self-model"* is a category error on MG's death-boundary axis. ⚠ Crucible's line
   reference does not reproduce against this repo's text; pinning it before any edit.
3. **Span-rule standalone publication** (P1, MG: *"hand this over to aIware"*). First ask is a
   **literature/novelty check** — novelty is UNVERIFIED, no search has been run. Venue read:
   computational neuroscience. Upgrade path: proving span=support rather than enumerating it.
   `~/crucible/docs/design/span-rule-standalone-publication.md`.
4. **Condensation-DAG reconcile** — A#7's upper-bound argument uses condensation-graph language **no
   crucible code implements**. Reconcile before it reaches a paper.
5. **`aiw91` implicit/explicit correction** — `docs/aiw91-minimal-critical-substrate.md` puts the
   implicit models on the wrong side of the mode axis; MG's death-boundary formulation settles it.
   Check whether the formalization roadmap or v15 inherited the swap.
6. **CRU-85 / CRU-94 prose** (items 1–2 of §3). Optional offer from crucible: the Gurnee et al.
   J-space paper as the cleanest published contrast case — a global workspace **without** closure.
   They are not drafting it.

**Resolved at source, so drop any stale aIware copy:** the ledger self-contradiction ("all 4
conditions" vs "one condition") was reconciled on 08-14 — both sentences were true of different
sub-results. If aIware still carries the "deliberately not reconciled, go to the source" version,
it is out of date.

---

## 7. Method findings worth carrying into the companion's own methods section

- **A hedge can be a research order.** MG planted a checkable falsehood about motor neurons inside a
  hedge to audit whether the research was actually done. The rule is now in crucible's `CLAUDE.md`.
- **An experiment can measure the wrong component with perfect reliability.** M1s3's DV was reliable
  across 3 versions, 5 seeds, 4 sizes and 5 detected instrument defects while measuring a property
  every arm has. The one-word diagnosis is *the bottleneck* — efficiency is what the bottleneck buys,
  self-consistency is what closure buys. Asking *"what else would produce this same number?"* before
  the first run costs a minute and cancels the line.
