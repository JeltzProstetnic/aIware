<!-- Action: reference. Tracked-by: AIW-123. Source: aIware WSL 2026-07-30/31 (Greek edition, review lane). -->
# AIW-123 — Greek (el) edition → publish-candidate (resume handoff)

**MG decisions (this session):** (1) run the FULL SOP pipeline (Opus/Fable multi-agent); (2) **STAGE to publish-candidate only** — MG arranges the native-Greek review before any PublishDrive push. Do NOT publish. el ≠ KDP → **PublishDrive/Ingram** (paperback), fonts fully embedded (Ingram gate).

## DONE this session
- **Interior-review reconciliation applied + committed (`c7cad188`)** — 5 High + 95 Med/Low AUTO fixes on `pop-sci/book-manuscript-el.md`, independently count-verified (0 collisions). Appendix lettering unified to **Latin**; loop→βρόχος (13 split, not global); Bekenstein bound/spelling; clinical terms; quote-marks «…»; the meaning-inverting dropped negation (l.1154 → «μη ανακτήσιμο»). Applier: `tmp/el-pipeline/apply_interior_fixes.py` + `tmp/el-pipeline/interior_fixes.json`.
- **9 interior HOLD items** deferred to MG — `tmp/el-pipeline/interior_fixes.json` (class=HOLD): number-format comma↔period (culture-guide-vs-review conflict — UNRESOLVED, MG call), Big Rip/Crunch EN-vs-Greek, black-hole register (μελανή οπή vs μαύρη τρύπα), DID "alter", γενεών→γενιών, anacoluthon rewrites, protected refrain.
- **Kalk scan DONE** (Fable, 90 editors, workflow `wf_0ab00bc1-0ec`) → **920 findings** persisted to `drafts/aiw108-el-kalk-findings.md` (A=394, B=509, D=12, C=5). Category-A fixes JSON: `tmp/el-pipeline/kalk_fixes_A.json`.
- **Infra staged + toolchain-verified:** Kalk `scripts/translation/kalk-el.js`, coherence `scripts/translation/coherence-el.js`, build `tmp/build_translation_interior_el.py` (**P052** = URW Palatino w/ full Greek — same family as siblings' Pagella; **polyglossia greek** hyphenation; italics kept; font-embed gate for Ingram; xelatex ×2). Smoke-tested clean (0 missing glyphs).

## REMAINING (in order) — resume here
1. **Apply Kalk category-A — REVIEW, don't blind-apply.** 394 A found (dry-run: 311 match clean, 80 quote-drifted vs the reconciled text, 3 ambiguous). The aggressive pass over-produced; triage a sample before applying. `python3 tmp/kalk_apply.py tmp/el-pipeline/kalk_fixes_A.json pop-sci/book-manuscript-el.md --dry` then apply, re-verify invariants (2475 ln, «…» pairs, Latin appendix refs). B(509)/D(12) → HOLD for MG; C(5) keep.
2. **Coherence pass (Opus):** `Workflow({scriptPath:"scripts/translation/coherence-el.js"})` — whole-book net. Apply el-only fix_quote/fix_replacement; log `structural` findings to AIW-109 (do NOT patch per-language). 
3. **Localize 3 figures** → `figures/{figure1-four-model-architecture-bw,figure2-real-virtual-split-bw,figure-five-layer-stack-bw}-el.svg` (translate labels via Fable; IWM/EWM/ISM/ESM + *qualia* stay Latin per glossary; template = the `-de`/`-ja` SVGs). Render PNG with cairosvg 1500px (Greek is in default fonts — NO CJK-style font injection needed; extend `tmp/render_localized_figures.py` LANGS to add "el"). **Then update the el manuscript's 3 inline image paths to `-el`** (currently base names: lines ~164/200/604 — the ja manuscript references `-ja`, so el must reference `-el`).
4. **Build:** `python3 tmp/build_translation_interior_el.py both` → `tmp/build-*/book-manuscript-el{,-hc}.pdf`. Script prints font-embed status + page count. Verify margins (`scripts/check_pdf_margins.py`). EPUB: `python3 tmp/build_book_epub_lang.py el` (make its CJK-vs-Latin font stack Greek-aware if needed — cf. the ZH font-variant bug in `pending-el-ko-zh-print-consolidation.md`).
5. **Stage publish-candidate:** cover (adapt `tmp/build_translation_covers_*.py` for el; **own ISBN required** — MG action, PublishDrive assigns none) + blurb/metadata. Assemble a `tmp/publishdrive-el-<date>/` kit. Present to MG for the native-Greek review gate.

## DO NOT
- Do NOT publish before MG's native-Greek pass (locked gate). Do NOT edit `.tex` directly. Do NOT recompile canonical EN/DE PDFs. Do NOT parallel-write the manuscript. tmp/ is gitignored — keepers are in `drafts/` + `scripts/translation/` (+ `tmp/build_translation_interior_el.py` is tracked via `git add -f`, like its siblings).
- ko edition still owes its entire Kalk+coherence+figures+build lane (same runbook, `docs/pending-translations-nl-el-ko.md`).
