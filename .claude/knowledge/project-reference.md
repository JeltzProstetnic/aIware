<!-- consumed-by: MEMORY.md (convenience mirror) -->
# aIware Project Reference — Durable Facts

Canonical home for the durable orientation facts mirrored in `MEMORY.md`. For live status see
`backlog.md`; for the full reference list see `references.md`.

## Theory (ultra-brief orientation)
- **Consciousness** = the ability to create a self-model, relate to it, and interact with it.
- **Four model *kinds* (2×2):** Implicit/Explicit × World/Self — IWM→EWM, ISM→ESM. Say "kinds," never "four modules" (see `.claude/knowledge/neuroscience-communication.md`).
- **Qualia** = a virtual "6th sense" of the simulation; exist only in the simulation, not the substrate → dissolves the hard problem.
- **Lineage:** MDM (Dennett) + SMT (Metzinger) + six-layer neural nets. **C4CA** = Class-4 cellular automaton shorthand for edge-of-chaos dynamics.
- German originals renamed to English in Session 10 (the conversation log preserves the originals).
- **Teaching devices / didactic patterns** (ocean, two dials of criticality, two causal roles, presence-vs-access, …) → registry `.claude/knowledge/didactic-patterns.md`; systematization tracked as `AIW-95`.

## "The Bubble Diagram"
- THE bubble diagram = `figures/figure2-real-virtual-split-simple.{svg,png}` — color, no "Key Insight" box. When Matthias says "the bubble diagram"/"bubble pic", use THIS file (not the BW version, not the architecture figure).
- SVG 780×560 viewBox; PNG render 2340×1680 (300 DPI at ~7.8").

## Author facts (canonical homes elsewhere — listed here for project lookup)
- **ORCID** 0009-0005-9697-1665 → canonical in `~/.claude/domains/life-management/career.md`.
- **Author photo** `/mnt/c/Users/Matthias/Pictures/1749406479497.jpg` → canonical in `~/.claude/foundation/user-profile.md`.
- **Math style** "pure intuitionist" (architectural intuition, not formal notation) → canonical in `~/.claude/domains/life-management/psych-profile.md`.
- **Uncle Bruno J. Gruber** — theoretical physicist (QM, symmetries), **ALIVE** (never write death dates), inspiration for the symmetry approach, acknowledged in the cosmology paper + book → canonical in `~/.claude/domains/life-management/family.md`.

## Key external paths
- **German monograph source PDF:** `/mnt/c/Users/Matthias/Documents/_Eigene/Die Emergenz des Bewusstseins 6x9 lit.pdf` (299pp; read via PyMuPDF/`fitz` — poppler not installed on WSL).

## AC implementation
- Migrated out of `~/aIware.implementation` (archived) → `~/mirror-box/` (Design 16, public) + `~/crucible/` (Design 15, private). Still research/publication phase; code is post-step-6. Designs: `docs/engineering/`. Tracked as backlog AIW-12.
